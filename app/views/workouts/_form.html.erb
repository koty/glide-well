<%= form_with model: @workout, local: true do |f| %>
  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>
  <div class="field">
    <%= f.label :kind %>
    <%= f.select :kind, [["swimming", 1], ["bike", 2]] %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
